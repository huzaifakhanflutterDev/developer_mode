import 'package:flutter/material.dart';

class DashboardTabItem extends StatelessWidget {
  const DashboardTabItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
