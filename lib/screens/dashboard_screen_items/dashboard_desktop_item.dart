import 'package:flutter/material.dart';

class DashboardDesktopItem extends StatelessWidget {
  const DashboardDesktopItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
