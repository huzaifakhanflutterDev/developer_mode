import 'package:flutter/material.dart';

class DashboardMobItem extends StatelessWidget {
  const DashboardMobItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
